<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Scratch 프로젝트 선택</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.5.0/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/public/styles.css">
    <script src="https://apis.google.com/js/api.js"></script> <!-- Google API 추가 -->
    <style>
        .btn-group {
            display: flex;
            justify-content: center; /* 가운데 정렬 */
        }

        .btn-group .btn {
            flex: 1;
            margin: 2px;
            border-radius: 10px;
        }

        .open-ppt {
            background-color: #f8f9fa;
            border: 1px solid #ced4da;
            padding: 3px 8px;
            border-radius: 5px;
        }

        .open-ppt:hover {
            background-color: #e2e6ea;
        }

        /* 제목 영역을 2줄로 고정하고 위쪽 정렬 */
        .card-title {
            text-align: left;
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
            max-height: 3rem; /* 두 줄 높이 */
            padding-right: 50px; /* 제목과 ppt 버튼 겹침 방지 */
            line-height: 1.2;  /* 위쪽 정렬을 위한 줄 간격 조정 */
            align-items: flex-start; /* 제목을 위쪽에 고정 */
        }

        /* C.T 학습 요소 영역 좌우를 꽉 채우고 동일한 여백 */
        .card-text {
            text-align: left;
            margin-top: 10px;
            margin-bottom: 10px;
            line-height: 1.2; /* C.T 학습 요소 간격 조정 */
            width: 100%; /* 카드 너비에 맞추어 꽉 채움 */
        }

        .position-absolute {
            position: absolute;
        }

        .top-0 {
            top: 0;
        }

        .end-0 {
            right: 0;
        }

        .m-2 {
            margin: 0.5rem;
        }

        /* 제목 영역 길이 증가 */
        .card {
            max-width: 22rem; /* 카드 너비 확장 */
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    
    <div class="container mt-5">
        <h1 class="text-center">Scratch 프로젝트 선택</h1>
        <p class="text-center">이 콘텐츠를 통해 재미있는 프로젝트를 경험해보세요</p>
        <div class="text-center mb-4">
            <a href="/scratch" class="btn btn-success">새로 시작하기</a>
        </div>
        <div class="row" id="content-container">
            <!-- sb3 파일 카드 목록이 여기에 동적으로 추가됩니다 -->
        </div>
    </div>
    
    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/public/header.js"></script>
    <script src="/public/scratch_project.js"></script>
</body>
</html>
