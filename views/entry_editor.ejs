<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Entry Editor</title>
    <style>
        html, body {
            margin: 0;
            padding: 0;
            width: 100%;
            height: 100%;
            overflow: hidden;
        }
        #entry-iframe {
            width: 100%;
            height: 100vh;
            border: none;
        }
    </style>
</head>
<body>
    <iframe id="entry-iframe" src="https://playentry.org/ws/" allowfullscreen></iframe>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const iframe = document.getElementById('entry-iframe');
            const projectFile = '<%= projectFile %>';
            
            if (projectFile) {
                iframe.src = `https://playentry.org/ws/?project_file=${encodeURIComponent(projectFile)}`;
            }
        });
    </script>
</body>
</html>