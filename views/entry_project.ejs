<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>엔트리 프로젝트 선택</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.5.0/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/styles.css">
    <script src="https://apis.google.com/js/api.js"></script>
    <script src="/learning-tracker.js"></script>
    <style>
        .btn-group {
            display: flex;
            justify-content: space-between;
        }

        .btn-group .btn {
            flex: 1;
            margin: 2px;
            border-radius: 10px;
        }
    </style>
</head>
<body>
    <%- include('partials/header') %>
    <input type="hidden" id="user-role" value="<%= role %>">
    <input type="hidden" id="user-id" value="<%= userID %>">
    <input type="hidden" id="center-id" value="<%= centerID %>">
    
    <script src="/public/js/entry/entry.min.js"></script>

    <div class="container mt-5">
        <h1 class="text-center">Entry 프로젝트</h1>
        <p class="text-center">다양한 Entry 프로젝트를 통해 프로그래밍 기술을 향상시켜보세요!</p>
        <div class="text-center mb-4">
            <a href="/entry" class="btn btn-success">새로 시작하기</a>
        </div>
        <ul class="nav nav-tabs" id="categoryTabs" role="tablist"></ul>
        <div id="content-container" class="tab-content"></div>
    </div>
    
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // 학습 트래커 초기화
            const pageTitle = document.title;
            const contentType = 'entry'; // 또는 'entry', 'computer' 등 페이지에 맞게 설정
            
            window.learningTracker = new LearningTracker(contentType, pageTitle);
            window.learningTracker.startTracking();
            
            // 프로그레스 업데이트 예시 (실제 구현에 맞게 수정 필요)
            function updateProgress(progress) {
                if (window.learningTracker) {
                    window.learningTracker.endTracking(progress);
                }
            }
        });
    </script>

    <script src="https://code.jquery.com/jquery-3.6.0.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/@popperjs/core@2.9.2/dist/umd/popper.min.js"></script>
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
    <script src="/header.js"></script>
    <script src="/js/entry_project.js"></script>
</body>
</html>
