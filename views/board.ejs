<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>일해라 너구리 - 너구리톡</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="/board.css">
    <link rel="stylesheet" href="/header.css">
    <script src="https://apis.google.com/js/api.js"></script>
</head>

<body>
    <!-- 공통 헤더 포함 -->
    <%- include('partials/header') %>

    <!-- 타이틀 & 소제목 (컨테이너 밖) -->
    <div class="container-fluid text-center mt-4">
        <h1 class="fw-bold">일해라 너구리</h1>
        <h3 class="chat-subtitle">필요한 것을 여기에 남기면, 너구리가 만듭니다.</h3>
    </div>

    <div class="container mt-4">
        <div class="row">
            <!-- 왼쪽: 채팅 컨테이너 -->
            <div class="col-md-6">
                <main class="direct-message">
                    <div class="nav">
                        <a href="/">
                            <i class="fa fa-arrow-circle-left" aria-hidden="true"></i>
                            <span class="screen-reader-text">뒤로 가기</span>
                        </a>
                        <h2>너구리톡 채팅</h2>
                        <a href="#">
                            <i class="fa fa-user" aria-hidden="true"></i>
                            <span class="screen-reader-text">프로필</span>
                        </a>
                    </div>

                    <div class="messages" id="chat-box">
                        <% posts.forEach(post => { %>
                            <div class="message-container <% if (user && user.username === post.author) { %> from <% } else { %> to <% } %>">
                                <img src="/default-profile.png" class="favicon">
                                <div class="message-content">
                                    <div class="message-author"><%= post.author %></div> <!-- 글쓴이 이름 -->
                                    <div class="message-bubble">
                                        <p><%= post.title %></p>
                                    </div>
                                    <div class="message-time"><%= post.created_at %></div> <!-- 글쓴 시간 -->
                                </div>
                            </div>
                        <% }); %>
                    </div>
                    

                    <!-- 입력창 -->
                    <div class="new">
                        <textarea class="new-input" id="chat-input" placeholder="한마디를 입력하세요 (최대 50자)" maxlength="50"></textarea>
                        <button type="submit" class="material-button" id="send-btn">
                            <i class="fa fa-comment" aria-hidden="true"></i>
                            <span class="screen-reader-text">보내기</span>
                        </button>
                    </div>
                </main>
            </div>

            <!-- 오른쪽: 업무 리스트 -->
            <div class="col-md-6">
                <div class="task-container">
                    <div class="text-center">
                        <img src="/resource/naguri.png" alt="너구리" class="naguri-img">
                    </div>
                    <h3 class="mt-3">현재 진행 중인 업무</h3>
                    <div id="task-list">
                        <!-- 업무 리스트가 여기에 동적으로 삽입됨 -->
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="/header.js"></script>
    <script src="/tasks.js"></script> <!-- 업무 리스트 로딩용 스크립트 추가 -->
    <script src="/chat.js"></script> <!-- 채팅 기능 추가 -->
</body>
