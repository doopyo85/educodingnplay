<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>일해라 너구리 - 너구리톡</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css">
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/bootstrap-icons/1.11.3/font/bootstrap-icons.min.css">
    <link rel="stylesheet" href="/styles.css">
    <link rel="stylesheet" href="/board.css">
    <link rel="stylesheet" href="/header.css">
    <script src="https://apis.google.com/js/api.js"></script>
</head>

<body>
    <!-- 공통 헤더 포함 -->
    <%- include('partials/header') %>

    <!-- 타이틀 & 소제목 (컨테이너 밖) -->
    <div class="container-fluid text-center mt-4">
        <h1 class="fw-bold">일해라 너구리</h1>
        <h2 class="chat-subtitle">너구리톡</h2>
    </div>

    <div class="container mt-4">
        <div class="row">
            <!-- 왼쪽: 채팅 컨테이너 -->
            <div class="col-md-6">
                <main class="direct-message">
                    <!-- 검색 폼 -->
                    <div class="search-box">
                        <form id="search-form" method="GET" action="/board/search">
                            <select class="search-type" id="search-type" name="searchType">
                                <option>글 한 마디</option>
                                <option>닉네임</option>
                                <option>해시태그</option>
                            </select>
                            <input type="text" placeholder="검색어..." id="search-value" name="searchValue">
                            <button type="submit"><i class="bi bi-search"></i></button>
                        </form>
                    </div>

                    <!-- 채팅 스타일 게시글 목록 -->
                    <div class="messages">
                        <% posts.forEach(post => { %>
                            <div class="message <% if (user && user.username === post.author) { %> from <% } else { %> to <% } %>">
                                <div class="favicon">
                                    <img src="/default-profile.png" class="chat-profile">
                                </div>
                                <p><%= post.title %></p>
                                <div class="time"><%= post.created_at %></div>
                                <% if (user && user.username === post.author) { %>
                                    <button class="material-delete" onclick="deletePost(<%= post.id %>)">✖</button>
                                <% } %>
                            </div>
                        <% }); %>
                    </div>

                    <!-- 입력창 -->
                    <div class="new">
                        <input type="text" class="new-input" id="chat-input" placeholder="한마디를 입력하세요 (최대 50자)" maxlength="50">
                        <button class="material-button" id="send-btn"><i class="bi bi-send"></i></button>
                    </div>
                </main>
            </div>

            <!-- 오른쪽: 업무 리스트 -->
            <div class="col-md-6">
                <div class="task-container">
                    <div class="text-center">
                        <img src="/resource/naguri.png" alt="너구리" class="naguri-img">
                    </div>
                    <h3 class="mt-3">현재 진행 중인 업무</h3>
                    <div id="task-list">
                        <!-- 업무 리스트가 여기에 동적으로 삽입됨 -->
                    </div>
                </div>
            </div>
        </div>
    </div>

    <script src="/header.js"></script>
    <script src="/tasks.js"></script> <!-- 업무 리스트 로딩용 스크립트 추가 -->
</body>
